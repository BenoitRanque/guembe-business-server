- args:
    name: available_listing_products
    table:
      name: available_listing
      schema: store
    using:
      manual_configuration:
        column_mapping:
          listing_id: listing_id
        remote_table:
          name: listing_product
          schema: store
  type: create_array_relationship
