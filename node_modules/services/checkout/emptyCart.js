const pg = require('utils/pg')

module.exports = async function emptyCart ({ user_id }, pgClient = null) {
  const db = pgClient === null ? pg : pgClient
  await db.query(`
    DELETE FROM webstore.cart_listing
    WHERE webstore.cart_listing.user_id = $1;
  `, [ user_id ])
}
